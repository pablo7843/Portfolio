---
const currentYear = new Date().getFullYear();
---

<footer class="system-footer">
  <div class="footer-top-divider"></div>
  
  <div class="footer-content">
    <div class="footer-section diagnostic">
      <div class="status-group">
        <span class="pulse-dot green"></span>
        <span class="status-label">SYSTEM_STATUS: <span class="highlight">OPTIMIZED</span></span>
      </div>
      <div class="status-group">
        <span class="pulse-dot blue"></span>
        <span class="status-label">ENCRYPTION: <span class="highlight">AES-256</span></span>
      </div>
    </div>

    <div class="footer-section identity">
      <p class="brand">PABLO_CLIMENT <span class="version">v2.0.26</span></p>
      <p class="copy">© {currentYear} ALL_RIGHTS_RESERVED</p>
    </div>

    <div class="footer-section links">
      <div class="social-grid">
        <a href="https://github.com/pablo7843" target="_blank" class="social-link">
          <span class="cmd">></span> GITHUB
        </a>
        <a href="https://www.linkedin.com/in/pabloclimentsanfelix" target="_blank" class="social-link">
          <span class="cmd">></span> LINKEDIN
        </a>
        <a href="mailto:pclimentsanfelix@gmail.com" class="social-link">
          <span class="cmd">></span> EMAIL
        </a>
      </div>
    </div>
  </div>

  <div class="footer-bottom-bar">
    <div class="terminal-loader">
      <span class="loading-text">LOCATING_USER...</span>
      <div class="mini-progress">
        <div class="mini-bar"></div>
      </div>
    </div>
  </div>
</footer>

<style>
  .system-footer {
    margin-top: 100px;
    background-color: var(--bg-dark);
    color: var(--text-secondary);
    font-family: 'Courier New', monospace;
    position: relative;
    border-top: 1px solid rgba(0, 255, 0, 0.1);
    padding-bottom: 20px;
  }

  .footer-top-divider {
    height: 1px;
    width: 100%;
    background: linear-gradient(90deg, transparent, var(--primary-color), transparent);
    margin-bottom: 40px;
  }

  .footer-content {
    max-width: 1200px;
    margin: 0 auto;
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 40px;
    padding: 0 20px;
    align-items: center;
  }

  /* Status Dots */
  .status-group {
    display: flex;
    align-items: center;
    gap: 10px;
    margin-bottom: 8px;
    font-size: 0.8rem;
  }

  .pulse-dot {
    width: 8px;
    height: 8px;
    border-radius: 50%;
  }

  .pulse-dot.green { background: #00ff00; box-shadow: 0 0 8px #00ff00; animation: blink 2s infinite; }
  .pulse-dot.blue { background: #0066cc; box-shadow: 0 0 8px #0066cc; animation: blink 1.5s infinite; }

  @keyframes blink { 50% { opacity: 0.3; } }

  .highlight { color: var(--primary-color); font-weight: bold; }

  /* Identidad */
  .identity { text-align: center; }
  .brand {
    font-size: 1.2rem;
    font-weight: bold;
    color: var(--text-primary);
    margin: 0;
    letter-spacing: 2px;
  }
  .version { font-size: 0.7rem; color: var(--primary-color); opacity: 0.6; }
  .copy { font-size: 0.7rem; opacity: 0.5; margin-top: 5px; }

  /* Links */
  .social-grid {
    display: flex;
    flex-direction: column;
    gap: 5px;
    align-items: flex-end;
  }

  .social-link {
    text-decoration: none;
    color: var(--text-secondary);
    font-size: 0.9rem;
    transition: all 0.3s ease;
  }

  .social-link:hover { color: var(--primary-color); transform: translateX(-5px); }
  .cmd { color: var(--primary-color); font-weight: bold; }

  /* Barra inferior */
  .footer-bottom-bar {
    margin-top: 40px;
    padding: 0 20px;
    display: flex;
    justify-content: center;
  }

  .terminal-loader {
    display: flex;
    align-items: center;
    gap: 15px;
    opacity: 0.4;
  }

  .loading-text { font-size: 0.65rem; }

  .mini-progress {
    width: 100px;
    height: 4px;
    background: rgba(255, 255, 255, 0.1);
    border-radius: 2px;
    overflow: hidden;
  }

  .mini-bar {
    width: 40%;
    height: 100%;
    background: var(--primary-color);
    animation: slide 3s infinite linear;
  }

  @keyframes slide {
    from { transform: translateX(-100%); }
    to { transform: translateX(250%); }
  }

  /* MODO CLARO */
  :global(html.light-mode) .system-footer {
    background: #f0f0f0;
    border-top: 1px solid #ddd;
  }

  /* RESPONSIVE */
  @media (max-width: 768px) {
    .footer-content { grid-template-columns: 1fr; text-align: center; }
    .social-grid { align-items: center; }
  }
</style>